<% if @city.nil? || @state.nil? %>
  <h4> Welcome to Match Play </h4>
<% else %>
  <div class="small-6 small-centered columns splash">
    <h2>
      <%= @city %>, <%= @state %>
    </h2>
  </div>
  <% unless @weather_data.empty? %>
    <% @weather_data.each do |hour| %>
      <div class="small-6 small-centered columns">
        <%= "#{hour[:temp]} °   " %>
        <%= image_tag "#{hour[:icon]}", class: 'weather-icon' %>
        <%= " #{hour[:pop]} %"%>
      </div>
    <% end %>
    <div class="small-6 small-centered columns splash">
    <h2>
      <%= "Sunset  #{@sunset}" %>
    </h2>
    </div>
    <div class="small-6 small-centered columns splash">

    </div>
  <% end %>
<% end %>
